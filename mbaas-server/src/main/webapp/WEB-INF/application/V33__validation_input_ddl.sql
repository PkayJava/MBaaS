# CREATE TABLE validation_input (
#
#   validation_input_id VARCHAR(100) NOT NULL,
#   input_id            VARCHAR(100) NOT NULL,
#   validation_id       VARCHAR(255) NOT NULL,
#
#   INDEX (input_id),
#   PRIMARY KEY (validation_input_id)
# );